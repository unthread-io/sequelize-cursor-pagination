{
  "scripts": {
    "check": "tsc",
    "build": "rm -rf build && tsc --project tsconfig.build.json",
    "test": "npm run build && node --test test/*.test.cjs"
  },
  "name": "@unthread-io/sequelize-cursor-pagination",
  "description": "Cursor pagination for Sequelize",
  "version": "0.0.2",
  "license": "MIT",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unthread-io/sequelize-cursor-pagination.git"
  },
  "keywords": [
    "sequelize",
    "pagination",
    "keyset pagination",
    "cursor pagination"
  ],
  "author": "Jake McCloskey",
  "bugs": {
    "url": "https://github.com/unthread-io/sequelize-cursor-pagination/issues"
  },
  "peerDependencies": {
    "sequelize": ">= 6.0.0 < 7.0.0"
  },
  "devDependencies": {
    "pg": "^8.23.0",
    "sequelize": "^6.29.0",
    "typescript": "^4.8.4"
  }
}
